## 1) Заголовок правки
Привести имена клиентов и подрядчиков в рабочих mock-данных и фильтрах
к именам из BASE_CLIENTS / BASE_CONTRACTORS; зафиксировать правило пополнения
BASE как единой точки входа для новых компаний.

## 2b) Локальная зона правки — файлы этой итерации

Символы, затронутые правкой:
- `client` (поле) в `src/mocks/projects.ts` — все 12 записей
- `clientName` (поле) в `src/mocks/kanbanMockData.ts` — записи id '1', '8', '10', '12'
- `CLIENT_DOC_TYPES` (ключи объекта) в `src/constants/index.ts:40`
- `clientOptions` (переменная) в `src/pages/ClientsPage.tsx:40`
- `clientOptions` (переменная) в `src/pages/ContractorsPage.tsx:83`
- `contractorOptions` (переменная) в `src/pages/ContractorsPage.tsx:87`
- `CLAUDE.md` — добавить правило о пополнении BASE

Импортёры, которых не трогаем (регрессия — см. раздел 4):
- `src/mocks/receivables.ts` / `src/mocks/receivables-dynamics.ts` /
  `src/pages/DynamicsTab.tsx` — там English-имена используются как ключи
  в отдельной финансовой логике; правка выходит за рамки этой итерации.

## 3) Описание правки

**Новые:**

**R9.** В `src/mocks/projects.ts` значения поля `client` для всех записей
совпадают точно со значениями `company` из `BASE_CLIENTS`:
`"Яндекс"`, `"Сбер"`, `"Авито"`, `"Альфа-Банк ТГ"`.
(Текущие значения: `"Yandex"`, `"Sber"`, `"Avito"`, `"Alfa-Bank TG"`.)

**R10.** В `src/mocks/kanbanMockData.ts` значения `clientName` у записей
id `'1'`, `'8'`, `'10'`, `'12'` заменены с `"Yandex"` на `"Яндекс"`.
Остальные записи уже содержат правильные русские имена.

**R11.** В `src/constants/index.ts` ключи объекта `CLIENT_DOC_TYPES`
переименованы под русские имена:
`Yandex` → `"Яндекс"`, `Sber` → `"Сбер"`, `Avito` → `"Авито"`,
`"Alfa-Bank TG"` → `"Альфа-Банк ТГ"`.
Поведение ClientsPage при выборе клиента не меняется — только ключи.

**R12.** В `src/pages/ClientsPage.tsx` строка 40 (`clientOptions`) заменена:
вместо `Array.from(new Set(projects.map((p) => p.client)))` использовать
`CLIENT_NAMES` из `@/utils/clients`. Добавить импорт `CLIENT_NAMES`.

**R13.** В `src/pages/ContractorsPage.tsx`:
- строка ~83 (`clientOptions`) заменена на `CLIENT_NAMES` из `@/utils/clients`;
- строка ~87 (`contractorOptions`) заменена на `CONTRACTOR_NAMES` из
  `@/utils/contractors`. Добавить импорты обеих утилит.
  Фильтр «Подрядчик» в ContractorsPage начнёт показывать всех подрядчиков
  из BASE, независимо от того, есть ли у них карточки в kanban-данных.

**R14.** В `CLAUDE.md` добавить раздел «Пополнение BASE» со следующим правилом:
Когда новый клиент или подрядчик добавляется в `baseClients.ts` /
`baseContractors.ts`, он автоматически появляется в фильтрах и дропдаунах
модальных окон (через `CLIENT_NAMES` / `CONTRACTOR_NAMES`).
Чтобы компания появилась в канбан-карточках и списке проектов — вручную
добавить хотя бы одну запись в `src/mocks/projects.ts` (для клиентов) или
`src/mocks/contractorsKanbanMockData.ts` (для подрядчиков).

## 3a) Трассировка

```
R9  → D17
R10 → D18
R11 → D19
R12 → D20, D17
R13 → D21, D22
R14 → D23
R5  → D1
R6  → D6
```

(R5 и R6 — из базового промпта; D1 и D6 проверяются как регрессия.)

## 4) Definition of Done (новое + регрессия)

**Новые:**

**D17.** Дропдаун «Клиент» в ClientsPage при открытии показывает 7 пунктов
на русском: «Яндекс», «Сбер», «Авито», «Альфа-Банк ТГ», «Тинькофф», «РусТех»,
«Альфа Медиа» (плюс «Все клиенты»). (R9, R12)

**D18.** В канбан-доске клиентов карточки с projectCode `YAN-1`, `YAN-2`,
`YAN-3` отображают `clientName` = `"Яндекс"` (не `"Yandex"`). (R10)

**D19.** `npm run typecheck` проходит без ошибок. (R11)

**D20.** `npm run build` проходит без ошибок. (R12)

**D21.** Дропдаун «Клиент» в ContractorsPage при открытии показывает те же
7 пунктов на русском, что и в D17. (R13)

**D22.** Дропдаун «Подрядчик» в ContractorsPage при открытии показывает все
10 подрядчиков из BASE_CONTRACTORS: «МедиаКрафт», «Инфлюенс Групп», «СтудияПро»,
«АдТех Solutions», «ИП Смирнов А.В.», «ООО МедиаСервис», «ООО Диджитал Про»,
«ИП Горелова Т.С.», «ООО Контент Лаб», «ИП Рябов Д.О.». (R13)

**D23.** В `CLAUDE.md` присутствует раздел «Пополнение BASE» с правилом,
описанным в R14. (R14)

**Регрессия:**

Регрессионные D взяты из базового промпта `prompts/03-clients-contractors-sync.md`,
трассировка R→D там же:

**D1** — CreateClientDocModal: дропдаун «Клиент» по-прежнему содержит все
7 клиентов из CLIENT_NAMES.

**D6** — CreateProjectModal: дропдаун «Клиент *» по-прежнему содержит все
7 клиентов из CLIENT_NAMES.

## 5) Проверка (новое + регрессионные шаги)

Проверка идёт через Playwright MCP по живому dev-серверу, не через
testing-library/jest/vitest.

1. Открыть http://localhost:5173, перейти в раздел «Согласования» → канбан
   клиентских документов. Кликнуть дропдаун «Клиент». Убедиться: 7 пунктов
   на русском, включая «Яндекс», «РусТех», «Альфа Медиа». [проверяет: D17]

2. Найти канбан-карточку с кодом проекта YAN-1. Убедиться: отображается
   «Яндекс», не «Yandex». [проверяет: D18]

3. Перейти в раздел «Подрядчики». Кликнуть дропдаун «Клиент». Убедиться:
   7 пунктов на русском (как в шаге 1). [проверяет: D21]

4. Кликнуть дропдаун «Подрядчик». Убедиться: присутствуют все 10 подрядчиков
   из BASE, включая «МедиаКрафт», «ИП Смирнов А.В.», «ООО Контент Лаб».
   [проверяет: D22]

5. Открыть CreateClientDocModal (кнопка «+ Создать» → «Документ клиента»).
   Открыть дропдаун «Клиент» — убедиться: 7 клиентов, включая «Яндекс».
   [проверяет: D1]

6. Открыть CreateProjectModal (кнопка «+ Создать» → «Новый проект»). Открыть
   дропдаун «Клиент *» — убедиться: 7 клиентов. [проверяет: D6]

7. Открыть `CLAUDE.md`, убедиться: раздел «Пополнение BASE» присутствует,
   содержит упоминание `projects.ts` и `contractorsKanbanMockData.ts`.
   [проверяет: D23]

8. В терминале: `npm run typecheck`. Убедиться: 0 ошибок. [проверяет: D19]

9. В терминале: `npm run build`. Убедиться: 0 ошибок. [проверяет: D20]

## 6) Финальная сверка

Прочитать `IMPLEMENTATION_REPORT.md` (секция итерации 1). Допущения базового
отчёта:
- Проект в CreateClientDocModal заменён на Input — не переигрывается.
- generateCode в CreateProjectModal упрощён — не переигрывается.

```
npm run lint
npm run typecheck
npm run build
```

Результат — новая секция в `IMPLEMENTATION_REPORT.md`:

## Итерация 2

| R-id | Статус | Где в коде | Комментарий |
|------|--------|------------|-------------|
| R9  | | src/mocks/projects.ts | |
| R10 | | src/mocks/kanbanMockData.ts | |
| R11 | | src/constants/index.ts | |
| R12 | | src/pages/ClientsPage.tsx | |
| R13 | | src/pages/ContractorsPage.tsx | |
| R14 | | CLAUDE.md | |

**Финальный статус итерации:** Готово / Готово с отклонениями / Заблокировано.

Критическое расхождение — исправить сразу. Некритическое — «реализовано с
отклонением», не округлять до «реализовано». Лимит 3 попытки на пункт
DoD за весь цикл, после — «заблокировано».

## Что НЕ включать

- Правку `src/mocks/receivables.ts`, `src/mocks/receivables-dynamics.ts`,
  `src/pages/DynamicsTab.tsx` — там English-имена используются как ключи
  в отдельной финансовой логике, это выходит за рамки этой итерации.
- Изменение `internalContractorsMockData.ts` — это отдельный раздел
  (внутренние подрядчики, хозяйственные расходы), не связанный с BASE_CONTRACTORS.
- Роутинг через react-router — не в scope.
- Реальный backend / Supabase-интеграцию.

## 7) Режим работы

ЗАПРЕЩЕНО задавать уточняющие вопросы или останавливаться в ожидании ответа в чате
на любом этапе выполнения задачи. При неоднозначности — выбери наиболее вероятную
трактовку, реализуй, опиши допущение только в финальном отчёте. Работай непрерывно
до конца чек-листа без остановок на подтверждение в чате. Исключение — системные
диалоги macOS/терминала (разрешение на запись файлов, bash-команды) — они не текстовые
вопросы, на них запрет не действует.
