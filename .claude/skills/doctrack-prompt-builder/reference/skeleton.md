<!--
  СКЕЛЕТ ПРОМПТА. Это шаблон для копирования, а не текст для чтения-и-пересказа.
  Скопируй файл в prompts/NN-название.md и заполни плейсхолдеры в квадратных
  скобках. Не меняй заголовки разделов, не меняй их порядок, не удаляй разделы.
-->

## 1) Заголовок

[Одно предложение: конкретный наблюдаемый результат, не категория действия.
 Плохо: "Улучшить сайдбар". Хорошо: "Добавить X, который делает Y при Z".]

---

## 2) Контекст и что НЕ трогать

[Репозиторий, где искать конвенции (project-knowledge skill).]

[Что уже реализовано и стабильно, с точными ссылками на символы/файлы,
 проверенными в коде только что: `функция/переменная` (`файл.tsx:строка на
 момент постановки`). Номера строк — вспомогательная подсказка, не якорь для
 последующей сверки; якорь — имя символа.]

[НЕ ТРОГАТЬ: конкретные файлы поимённо. Не общими категориями вроде
 "существующий код".]

[Явно: чего в кодовой базе НЕТ и что не создаётся в этой задаче, если это
 может быть спутано с чем-то похожим по названию.]

---

## 3) Описание задачи

**R1.** [Требование — проверяемая величина, не прилагательное.]

**R2.** [...]

[Если требование может пересечься с другим UI-элементом с похожим текстом/
 названием — адресуй его через конкретный идентификатор (data-testid, имя
 символа), не через текст, который может встретиться в интерфейсе дважды.]

[Завершающий пункт раздела 3: построчное сопоставление реализации с
 требованиями, указание файла `IMPLEMENTATION_REPORT.md` как единого файла-
 отчёта, дополняемого в разделе 6.]

---

## 3a) Трассировка требований (R → D)

---

## 4) Definition of Done

**D1.** [Бинарный критерий: да/нет, один факт. Без "или" и без перечисления
 вариантов реализации через "/".]

**D2.** [...]

[Отсутствие чего-либо формулируется так же явно, как присутствие: не
 "элемент выглядит скрытым", а "элемент отсутствует в DOM, проверено через
 снапшот".]

---

## 5) Проверка через Playwright (MCP, реальная браузерная сессия)

[Проверка в терминах реального DOM/UI, не тестового рантайма: нет
 `toBeInTheDocument`/`queryByText`/`queryByRole` — вместо них: снапшот DOM,
 подсчёт узлов по `data-testid`/атрибуту, скриншот с конкретным критерием.]

1. [Действие → скриншот/снапшот → конкретная проверка.] [проверяет: D1]
2. [...]

[Если состояний несколько (пустое/заполненное/разные роли и т.п.) — покрыт
 каждый релевантный переход отдельным шагом, а не один пример вместо всех.]

[Последний шаг — проверка консоли на 0 ошибок в точке, где это реально
 читается (после конкретного шага, не ретроспективно за весь список).]

[Если задача без UI — здесь явно описана замена Playwright-протокола, не
 молчаливый пропуск раздела.]

---

## 6) Финальная сверка с ТЗ

Отдельный проход после прохождения раздела 4 — построчная сверка с разделом 3,
не повтор раздела 5.

1. Пройти R1…Rn по коду буквально, не по памяти о процессе реализации.
2. Сверить `git diff` с разделом 2 — изменения не вышли за пределы
   перечисленных файлов.

**Каскад:** если формулировка раздела 3 скорректирована в ходе сверки —
заново пройти 3a для этого R-id, соответствующие D-id, соответствующие шаги
раздела 5.

Обновление `IMPLEMENTATION_REPORT.md`:

| R-id | Статус | Где в коде | Комментарий |
|---|---|---|---|

**Протокол расхождений:**
1. Критическое (нарушает DoD/меняет поведение из раздела 3) — исправить сразу.
2. Некритическое — зафиксировать как «реализовано с отклонением», не округлять
   до «реализовано».
3. Лимит: не более 3 попыток на один пункт DoD за весь цикл (раздел 5 + 6).
   После 3 — «заблокировано», с описанием, что не выходит.

**Финальный статус:** Готово / Готово с отклонениями / Заблокировано —
без промежуточных формулировок.

---

## 7) Режим работы

[Дословно из reference/section7_canonical.txt]

---

## Что НЕ включать

[Явный список того, что не входит в скоуп, но могло бы напрашиваться — код
 под будущие задачи, поля/типы, которые ничего не читают в рамках этой
 задачи, и т.п. Если такого нет — написать "дополнительных ограничений нет",
 заголовок не убирать.]